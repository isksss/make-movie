[package]
name = "mm-plugin-rust-basic"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
mm-sdk-rust = { path = "../../sdk/rust" }

[workspace]
